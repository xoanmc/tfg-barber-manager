spring:
  datasource:
    host: localhost
    port: 5432
    database: tfg
    username: tfg
    password: tfg
    url: jdbc:postgresql://${spring.datasource.host}:${spring.datasource.port}/${spring.datasource.database}
    hikari:
      connectionTimeout: 20000
      maximumPoolSize: 5
  jpa:
    open-in-view: false
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      encoding: UTF-8
      mode: always
      continue-on-error: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: pepe11.perez91@gmail.com
    password: dhvc hfet nieo afpr
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  task:
    execution:
      pool:
        core-size: 4
        max-size: 10
        queue-capacity: 100
logging:
  pattern:
    console: "%d{yyyy-MMM-dd HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{15}) - %msg %n"
  level:
    root: INFO
    com.example.demo: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
properties:
  clientHost: http://localhost:1234
  jwtSecretKey: iuabhsdfiasddfioasddfoa7w9earh324982h397239u23badswfasd2i9238723
  jwtValidity: 7200
  upload:
    path: D:\TFG\tfg-barber\tfg\servidor\uploads # ruta donde se guardaran los archivos de subida
app:
  base:
    url: http://localhost:8080
